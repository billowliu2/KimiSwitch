# 工作交接（2026-08-01 第二轮）

> 新会话续作入口：先读本文件，再按「六、下一步」继续。历史交接见 `docs/HANDOVER-usage-billing.md`（账单功能，已发布完）。

## 一、当前未完成任务（按优先级）

1. **官网截图归档**（`docs/screenshots/site-2026-08-01/`，14 张 Snipaste 图）：
   - **当前模型无视觉输入，无法识图**——需用户在会话里切换到支持视觉的模型后逐个查看
   - 确认每张对应界面 → 按官网区块重命名（hero / feature-usage / kimi-oauth / settings…）
   - 注意：`.gitignore` 忽略 `Snipaste_*.png`，这些图不进 git，归档目录也只是暂存
2. **官网规划待拍板**（`docs/WEBSITE-PLAN.md` v2，3 项待定）：
   - 素材时机：等 v0.7.0 稳定版（推荐）还是用当前 v0.6.x 截图
   - 技术栈：Vite + React + Tailwind（已写入方案，等确认）
   - 推荐链接：官网页脚放基元律动等推荐链接，还是不放

## 二、v0.6.7 发布状态详情（已完成）

- **提交**：`ce28252` feat: Kimi 设备授权登录（应用内置 kimi login）+ 外链打开统一走 Rust 命令（v0.6.7）
- **版本**：三处 0.6.7（package.json / Cargo.toml / tauri.conf.json），`check-version.sh 0.6.7` 通过
- **两仓库**：origin（git.codingplan.site/admin/KimiCodeSwitch）+ github（billowliu2/KimiSwitch）的 main / Dev_20260801 / tag v0.6.7 全部同步
- **私有库 Release**：https://git.codingplan.site/admin/KimiCodeSwitch/releases/tag/v0.6.7（MSI 6.9MB）
- **GitHub Release**：https://github.com/billowliu2/KimiSwitch/releases/tag/v0.6.7（三平台构建成功，6 个产物齐全，已转正式）
- **本地包**：`src-tauri/target/release/bundle/{nsis,msi}/Kimi Switch_0.6.7_*`
- **v0.6.7 功能**：Kimi 设备授权登录（Device Code Flow，等同 `kimi login`）——`kimi_oauth_start` / `kimi_oauth_poll` 命令 + `KimiOAuthDialog` 前端；授权页/设置页外链统一改走 Rust `open_external_url` 命令（JS 插件版 openUrl 在此环境静默失败，已弃用）

## 三、关键环境与凭据获取

- **代理**：git 全局 `http.proxy=http://127.0.0.1:7897`；推 origin 需 `git -c http.proxy= push` 绕过（schannel 握手失败）；调 GitHub API 必须 `-x http://127.0.0.1:7897`
- **GitHub API token**：`printf "protocol=https\nhost=github.com\n\n" | git credential fill` → password 行（临时文件 /tmp/ghcred.txt 可能已失效，重新获取）
- **私有库 API 凭据**：同上 host=git.codingplan.site（/tmp/pcred.txt）
- **models.dev 同步**：`npm run fetch-models-dev`（脚本自动读 git http.proxy 走代理）；打包前 `pretauri` 钩子自动同步；离线时用本地已提交 `src/lib/models-dev.json` + last-good 备份恢复
- **版本发布流程**：check-version → tauri build → commit → 推两仓库 → 私有库 API 建 Release+传 MSI → 推 github tag → CI 完成转正式

## 四、官网规划要点（docs/WEBSITE-PLAN.md v2）

- 定位：多供应商统一管理 + 用量监控（非 Kimi 单一产品）
- 部署：仅 GitHub Pages（**私有库 Gitea Pages 实测未启用**，勿再做镜像方案）
- 技术栈：Vite + React + Tailwind（独立 `website/` 子目录）
- 结构：Hero（供应商列表截图）→ 功能 6 卡 → 性能 → 更新 → 隐私 → 平台 → CTA
- 素材：真实截图+动图；性能指标需实测后如实写

## 五、已有交接/文档

- `docs/HANDOVER-usage-billing.md`：账单查询功能全量交接（已随 v0.6.4-v0.6.6 发布）
- `docs/USAGE-QUERY-ADAPTATION.md`：账单查询调研
- `docs/WEBSITE-PLAN.md`：官网规划 v2
- `docs/screenshots/site-2026-08-01/`：14 张待归档产品截图（需视觉模型）

## 六、下一步 TODO

1. 查看后台轮询结果（`TaskList` / `TaskOutput` 或读 `/tasks`）：v0.6.7 GitHub CI 完成后转正式 Release
2. 用户切视觉模型后：识图 → 重命名归档 14 张截图 → 更新素材清单
3. 用户对 3 个待拍板项确认后更新 WEBSITE-PLAN.md
4. 后续：官网 M0-M4 里程碑（等拍板 + 稳定版）
