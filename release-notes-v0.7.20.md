# KimiSwitch v0.7.20

## 新增

- **套餐用量阈值预警**：用量配置面板新增「预警阈值 %」（0-100，0 或空 = 关闭）。每次账单查询后，任一百分比用量窗口达到阈值即红色高亮 + ⚠ 标记，低于阈值自动消除；自动查询间隔会周期性重复评估
- **Kimi For Coding 加力钱包**：0.43.1 起服务端 quota 响应中的 `boosterWallet`（加力钱包）现在解析展示为一行余额（💰 月度用量 / 总额 / 余额，含币种），与四个用量窗口并列
- **供应商凭证环境变量化（`api_key_env`）**：适配 kimi-code 2.0.0 #3762——供应商编辑页「凭证来源」可切换「直接填写 / 环境变量」，密钥不写进 config.toml；导出时与 `api_key` 互斥；账单查询 / 获取模型列表会实时解析环境变量（变量未设置给出明确错误）；SQLite 新增列（含自动迁移）
- **Kimi Code 版本跟踪器**：设置页新增「Kimi Code 版本」卡片——本机已装版本（`kimi --version` 检测）、上游最新版本、适配状态徽标（绿已适配 / 黄建议升级 KimiSwitch / 橙部分兼容 / 灰未安装），可一键打开上游 Releases
- **供应商一键体检**：供应商列表新增「一键体检」按钮——并发探活所有启用供应商的推理端点（`GET /v1/models`）并顺带重新查询账单，每行状态点 + 延迟 tooltip + 顶部汇总。状态判定为三态：账单或探活任一真实通过 = 绿；鉴权失败必红；端点不支持模型列表（404）计中性不误报

## 设置

- **压缩重试次数**：Agent 设置新增 `loop_control.compaction_max_attempts`（上游 0.43.0 #3750，默认 5，0/空不写键）
- **文件监听开关**：新增 `[watch] enabled` 开关（上游 2.0.1 #3892，2.0.2 起上游默认关闭），并探测 `KIMI_CODE_WATCH` 环境变量

## 适配

- **kimi-code 2.0.2 已适配确认**：wire 记录格式、实验 flag（5 个）、config 模式均无变化；`api_key_env` / `[watch]` / `[database]` 新增配置节经现有合并式写入验证无损往返

## 备注

- 版本跟踪器的「最新版本」查询直连 GitHub，受限网络环境下可能显示 `—`（已装版本与状态判定不受影响）
- 体检跳过大模型平台类供应商（anthropic / google-genai / vertexai 无 `/v1/models` 路由），显示灰点「未检测」
